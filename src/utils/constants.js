export const LOCAL_STORAGE_KEYS = Object.freeze({
  NotesCategories: "notes-blaslkdj",
});

export const COOKIE_KEYS = Object.freeze({
  AccessToken: "access-token",
});

export const SIZES = Object.freeze({
  Small: 1,
  Medium: 2,
  Large: 3,
});
